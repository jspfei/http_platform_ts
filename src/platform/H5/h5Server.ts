import {IServer, IServerUserBaseInfo} from "../Server";

export class ccServer implements IServer {
    saveToServer(data: IServerUserBaseInfo) {
    }

    tickToServer(data: IServerUserBaseInfo){
    }

}